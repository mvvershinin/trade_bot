# Где остановились

Файл живёт ради одного: **сессия не должна расти дальше 300k токенов.**
Замер 14.09.2026 — [`quality/context-cost-2026-09-14.md`](quality/context-cost-2026-09-14.md):
три самые длинные сессии дали 64 % всего чтения кэша, а порог 300k срезает
61 % контекста и 44 % расхода.

Приём: **допиши сюда → `/clear` → новая сессия читает этот файл и продолжает.**
Про порог напомнит хук `context-budget-guard.sh` и статуслайн.

Пишется коротко и по делу: что сделано, что следующее, чего не знает тот,
кто придёт после. Разбор и обоснования — не сюда, а в `quality/` и `decisions/`.

---

## 14.09.2026 — четыре красных сторожа на бегунке (`B-049`)

**Чем оказалось.** Кнопка загрузки истории работает. Сторожа протухли по календарю:
`tests/test_ui_history_load.py` держал `TODAY = date(2026, 9, 6)` и строил ответы
подставной биржи вокруг этого дня, а `app/port.py` считал отрезок загрузки
от системных часов. Когда сегодняшний день уехал правее подставных данных,
загрузчику стало нечего просить. Разбор и карта по дням —
[`quality/calendar-rot-2026-09-14.md`](quality/calendar-rot-2026-09-14.md).

✅ **Сделано**
✅ `HistoryPort` получил довод `clock` — источник «сейчас», умолчание прежнее
✅ четыре сторожа переписаны на управляемый день, `B-049` закрыт
✅ три новых сторожа на саму болезнь: путь кнопки в 2024, 2026 и 2031 году
   и равенство правой границы запроса названному дню
✅ замер всей сюиты сдвигом системных часов: `3939 passed` при +137 днях
✅ инструмент замера положен в `quality/tools/shiftclock.c`, собирается одной командой
✅ полный прогон локально: `3984 passed, 1 skipped`; ruff и mypy — числа те же, что до правки

⛔ **Свой же вывод опровергнут в тот же день — читать первым**
⛔ «других сторожей на календаре машины нет» — неправда: замер снят на одном сдвиге,
   +137 дней, и этого мало. На +310 днях краснеют 4 проверки в `tests/test_backtest_main.py`,
   на −137 — 28 в `tests/test_app_backfill.py`
⛔ сюита покраснеет сама по себе **21.07.2027**, день найден бисекцией; разбор — `D-112`
⛔ первый инструмент замера дал 488 падений, из которых 485 оказались его собственной
   поломкой; поймано контрольным прогоном с нулевым сдвигом за 21 секунду

⬜ **Не сделано, ждёт решения владельца счёта**
⬜ `D-110` — 356 проверок показывают имена эскейпами `\u0441...` в логе бегунка:
   кириллица в `ids`, нарушение правила 6. Одной строкой не чинится (24 ошибки сбора)
⬜ прогон сюиты со сдвинутыми часами раз в неделю на бегунке — ловит протухание
   за полгода до срока, стоит двадцать секунд, только Linux
⬜ коммит и push: CI на PR #10 красный ровно этими четырьмя, локально зелено

## 14.09.2026 — расход контекста

**Сделано.** Замер расхода за 13 дней и план мер:
[`quality/context-cost-2026-09-14.md`](quality/context-cost-2026-09-14.md),
[`plans/context-cost-reduction.md`](plans/context-cost-reduction.md).
Две гипотезы записки `OPTIMIZATION-NOTE.md` замер не подтвердил (`CLAUDE.md` — 1,7 %
расхода, промпты агентов — 0,03 %), третью подтвердил (длина сессии).
Встречный замер сессии `life/.mounted` на своём наборе дал то же: контекст
на запрос 367k против названных 62k, топ-3 сессии — 58 % чтения кэша,
`CLAUDE.md` — 0,9 %.

**Поставлено по решению владельца счёта 14.09.2026 — три меры хуками, не правилами:**

✅ `bash-output-guard.sh` (PreToolUse: Bash) — дамп файла больше 20 000 байт
без ограничителя блокируется с объяснением. Узкий диапазон `sed -n`, `head`,
`tail`, `wc`, `grep` и запись файлов (heredoc, перенаправление) не трогает.
Явный выход, если файл нужен целиком: `| head -n 100000`.
✅ `context-budget-guard.sh` (Stop) — после 300k показывает строку с текущим
размером контекста и напоминанием про этот файл. Не блокирует.
✅ статуслайн показывает абсолютные токены и краснеет после порога:
было `[30%]`, стало `[312k / 31%]`.

⛔ **Мера 3 отменена по замеру.** «Повторное чтение файлов — 19 % выдачи `Bash`»
не подтвердилось: дословных повторов 0,2 %, а 226 «повторов» были разными кусками
одного файла. Проверка на повтор из хука снята — она блокировала бы чтение
второго куска, то есть то, что сама советует в тексте отказа.

**Не сделано, ждёт решения.** Мера 4 — уборка `CLAUDE.md` с 504 строк до ~200
(ради читаемости, по деньгам это 1 %). Строка про `OPTIMIZATION-NOTE.md`
в шапке `CLAUDE.md` снимается по итогам обсуждения. Строка в `ROADMAP.md`
на план — за владельцем счёта (правило 10).

**Что не изменилось.** Приёмка глазами на машине владельца счёта не проведена
(см. «Текущая задача» в `CLAUDE.md`). Работа за двое суток лежит в PR #10.
Замер снял одно возражение против приёмки: снимки экрана стоят 3 % трафика,
экономить на них нечего.

---

## Замечено по ходу 14.09.2026

**Дыра в предохраннике, закрытая 14.09.2026.** Хук получает текст команды
**до подстановки переменных шелла**: `cat $F` приходит так и есть, литерального
файла с именем `$F` на диске нет, и звено молча отбрасывалось. То есть чтение
через переменную проходило насквозь. Развернуть переменную предохранитель
не может — это было бы исполнением команды. Закрыто иначе: **не видишь файл —
требуй ограничитель**, тогда потолок выдачи известен независимо от того, что
за файл. Нашла встречная проверка сессии `life/.mounted`.

**Хуки подхватываются в идущей сессии, снапшота на старте нет.** Проверено
на живом пути: регистрация в `.claude/settings.local.json` посреди сессии —
и `cat` крупного файла тут же получает отказ. Правки тела скрипта тоже
действуют сразу: в настройках лежит путь, а не содержимое. Сессия
`life/.mounted` утверждала обратное на своём опыте — у неё вероятнее
относительный путь из глобальных настроек либо запас в один байт над порогом.

