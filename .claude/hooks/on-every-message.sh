#!/bin/bash
# UserPromptSubmit — напоминание правил при каждом сообщении.
# Claude забывает правила в длинных сессиях. Хук исполняет харнесс, а не модель.

cat <<'REMINDER'
⚡ НАПОМИНАНИЕ (хук on-every-message):
1. КРИТИК — после кода обязательно /review. Нашёл проблемы — исправь и перепроверь, не спрашивая.
2. ТОРГОВАЯ ЛОГИКА — тронул вход/выход/окно/тейк/переворот → обязательна /parity (сверка с прототипом).
3. ДЕНЬГИ — тронул заявки/объём/ГО/токен → обязательна /risk.
4. АРХИТЕКТУРА — движок не знает, откуда пришла свеча. Никаких if режим==тестер в engine/ и strategies/.
5. ОТКРЫТЫЕ ВОПРОСЫ — нет ответа заказчика в .docs/decisions/ → код не пишем, зовём /roadmap.
6. НЕТ ОПТИМИЗМУ — никаких «Готово!», «Отлично». Только факты и проблемы. Не проверил — так и скажи.
REMINDER

exit 0
