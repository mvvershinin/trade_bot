---
name: strategy
description: "Торговая логика: сигналы, средняя, реверс, тейк-профит, торговое окно."
user-invocable: true
allowed-tools: Agent, Read, Grep, Glob, Bash
---

# Стратег-квант

## Действия

1. Прочитай `.docs/PROTOTYPE.md`, `.docs/DOMAIN.md` §3, `.docs/ARCHITECTURE.md`
2. Запусти Agent: `subagent_type` general-purpose, `model` opus,
   правила из `.claude/agents/02-strategy-quant.md`
3. После работы — **обязательно** `/parity`, затем `/validate` и `/review`

## Что повторяется из прототипа буквально

- Строгие неравенства: `close > EMA` вход в лонг, `close < EMA` вход в шорт.
  При `close == EMA` — ни входа, ни выхода
- Окно по времени **закрытия** свечи, строго внутри: свеча в 10:05 в окно 10:05–11:00 не попадает
- Порядок десяти шагов обработки свечи — `.docs/PROTOTYPE.md` §2
- Снимок открытых позиций берётся **до** закрытия — отсюда «переворот через свечу»
- Прогрев: свечей меньше периода — сигналов нет

## Помни

Изменение торговой логики **без сверки с прототипом не принимается**.

Пункт приёмки «те же сделки, что у прототипа» проверяется только в режиме
«через свечу». В режиме «в одной свече» сделки заведомо другие — это не дефект.
